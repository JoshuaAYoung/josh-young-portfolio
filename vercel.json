{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/verify-recaptcha",
      "dest": "/api/verify-recaptcha.ts"
    }
  ]
}
